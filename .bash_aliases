# enable color support
export CLICOLOR=1

# some ls aliases
alias ll='ls -l'
alias la='ls -a'
alias l='ls -cfal'

# docker aliases
alias dk='docker'
alias dkc='docker compose'
alias dkce='docker compose exec'
